<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nbapp">

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddIncomeActivity" />
        <activity android:name=".AddExpendActivity" />
        <activity android:name=".DetailActivity">

        </activity>
        <activity android:name=".FindActivity" >

        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".UserActivity">
            <action android:name="com.example.nbapp.ACTION_STAERT" />

            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.LOGINTOUSER" />
        </activity>
        <activity
            android:name=".EditIncomeActivity"
            android:label="@string/title_activity_edit_income"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditExpendActivity"
            android:label="@string/title_activity_edit_expend"
            android:theme="@style/AppTheme" />i
        <activity android:name=".BilActivity">

        </activity>
    </application>

</manifest>